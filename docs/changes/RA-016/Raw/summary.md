Bạn là AI engineer trong dự án “AI Smart Resource Allocation & Bench Prediction System”.


Hãy kiểm tra component ActiveAllocationsTable trong file apps/web/features/allocation/ActiveAllocationsTable.tsx. Hiện tại, hai nút Edit và Remove chưa hoạt động vì chưa có logic xử lý.

Điều tra xem component này được sử dụng ở đâu trong codebase.
Đảm bảo rằng ở nơi sử dụng, props onEdit và onRemove được truyền vào với các hàm xử lý phù hợp (ví dụ: mở form edit gọi API chỉnh sửa,xác nhận xóa, gọi API xóa, v.v).
Nếu chưa có, hãy đề xuất hoặc tạo các hàm xử lý này và truyền vào component.
Giải thích rõ các thay đổi cần thực hiện và chỉ ra file nào cần sửa.
Đảm bảo sau khi sửa, hai nút Edit và Remove hoạt động đúng chức năng.



